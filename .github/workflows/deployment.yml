name: Deployment Pipeline
run-name: Deployment Pipeline ${{ inputs.source_repository }}

on:
  workflow_dispatch:
    inputs:
      source_repository:
        required: true
        type: string
        description: ""

      actor:
        required: true
        type: string
        description: ""

      repo_path:
        required: true
        type: string
        description: ""

      container_names:
        required: false
        type: string
        default: ""
        description: ""

      run_pull_changes:
        required: false
        type: boolean
        default: true
        description: ""

      run_stop_compose_containers:
        required: false
        type: boolean
        default: false
        description: ""

      run_build_docker_compose_file:
        required: false
        type: boolean
        default: false
        description: ""

      run_start_compose_containers:
        required: false
        type: boolean
        default: false
        description: ""

      run_restart_non_compose_containers:
        required: false
        type: boolean
        default: false
        description: ""
        
      health_check:
        required: false
        type: string
        default: ""
        description: "URL|timeout_seconds|expected_status, e.g. https://host:443/health|180|200. Empty means no check."

jobs:
  check-authorization:
    runs-on: ubuntu-latest
    steps:
      - name: Check authorization
        run: |
          ADMIN_USERS="${{ secrets.ADMIN_USERS }}"

          if [[ ! " $ADMIN_USERS " =~ " ${{ inputs.actor }} " ]]; then
            echo "User ${{ inputs.actor }} is not authorized to run this workflow."
            exit 1
          fi

  deploy-changes:
    if: >-
      ${{
        inputs.run_pull_changes &&
        always() &&
        needs.check-authorization.result == 'success'
      }}
    needs:
      - check-authorization
    runs-on: ubuntu-latest
    steps:
      - name: Pull from Git
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.NAS_HOST }}
          port: ${{ secrets.NAS_SSH_PORT }}
          username: ${{ secrets.NAS_USER }}
          password: ${{ secrets.NAS_SSH_KEY }}
          script: |
            cd "${{ inputs.repo_path }}"
            /opt/bin/git pull

  stop-compose-containers:
    if: >-
      ${{
        inputs.run_stop_compose_containers &&
        always() &&
        needs.check-authorization.result == 'success' &&
        (
          !inputs.run_pull_changes ||
          needs.deploy-changes.result == 'success'
        )
      }}
    needs:
      - check-authorization
      - deploy-changes
    runs-on: ubuntu-latest
    steps:
      - name: Stop compose containers
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.NAS_HOST }}
          port: ${{ secrets.NAS_SSH_PORT }}
          username: ${{ secrets.NAS_USER }}
          password: ${{ secrets.NAS_SSH_KEY }}
          script: |
            cd "${{ inputs.repo_path }}"
            # --remove-orphans: Ohne das Flag ueberlebt ein Container, dessen
            # Dienst aus der compose-Datei entfernt wurde, jedes `down` und
            # `up -d` -- Compose kennt ihn nicht mehr und fasst ihn deshalb
            # nicht an. Er laeuft dann mit dem alten Image weiter, belegt
            # seinen Port und sieht fuer jede Ueberwachung wie ein gesunder
            # Dienst aus. Genau so ueberlebte InfraMonitor-SnmpExporter am
            # 09.08.2026 seinen eigenen Ausbau und musste von Hand entfernt
            # werden.
            #
            # Der Wirkungsbereich ist eng: Betroffen sind ausschliesslich
            # Container desselben Compose-Projekts, also solche, die frueher
            # einmal in dieser compose-Datei standen. Container aus anderen
            # Projekten oder von Hand gestartete fasst Compose nicht an.
            #
            # Kein eigener Schalter dafuer: workflow_dispatch erlaubt hoechstens
            # zehn Eingaben, und die sind bereits vergeben (siehe den Hinweis
            # am health_check-Eingang).
            echo "${{ secrets.NAS_SSH_KEY }}" | sudo -S /usr/local/bin/docker-compose down --remove-orphans

  build-docker-compose-file:
    if: >-
      ${{
        inputs.run_build_docker_compose_file &&
        always() &&
        needs.check-authorization.result == 'success' &&
        (
          !inputs.run_pull_changes ||
          needs.deploy-changes.result == 'success'
        ) &&
        (
          !inputs.run_stop_compose_containers ||
          needs.stop-compose-containers.result == 'success'
        )
      }}
    needs:
      - check-authorization
      - deploy-changes
      - stop-compose-containers
    runs-on: ubuntu-latest
    steps:
      - name: Build docker image
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.NAS_HOST }}
          port: ${{ secrets.NAS_SSH_PORT }}
          username: ${{ secrets.NAS_USER }}
          password: ${{ secrets.NAS_SSH_KEY }}
          script: |
            cd "${{ inputs.repo_path }}"
            echo "${{ secrets.NAS_SSH_KEY }}" | sudo -S /usr/local/bin/docker-compose build

  start-compose-containers:
    if: >-
      ${{
        inputs.run_start_compose_containers &&
        always() &&
        needs.check-authorization.result == 'success' &&
        (
          !inputs.run_pull_changes ||
          needs.deploy-changes.result == 'success'
        ) &&
        (
          !inputs.run_stop_compose_containers ||
          needs.stop-compose-containers.result == 'success'
        ) &&
        (
          !inputs.run_build_docker_compose_file ||
          needs.build-docker-compose-file.result == 'success'
        )
      }}
    needs:
      - check-authorization
      - deploy-changes
      - stop-compose-containers
      - build-docker-compose-file
    runs-on: ubuntu-latest
    steps:
      - name: Start compose containers
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.NAS_HOST }}
          port: ${{ secrets.NAS_SSH_PORT }}
          username: ${{ secrets.NAS_USER }}
          password: ${{ secrets.NAS_SSH_KEY }}
          script: |
            cd "${{ inputs.repo_path }}"
            echo "${{ secrets.NAS_SSH_KEY }}" | sudo -S /usr/local/bin/docker-compose up -d

  restart-non-compose-containers:
    if: >-
      ${{
        inputs.run_restart_non_compose_containers &&
        inputs.container_names != '' &&
        always() &&
        needs.check-authorization.result == 'success' &&
        (
          !inputs.run_pull_changes ||
          needs.deploy-changes.result == 'success'
        ) &&
        (
          !inputs.run_stop_compose_containers ||
          needs.stop-compose-containers.result == 'success'
        ) &&
        (
          !inputs.run_build_docker_compose_file ||
          needs.build-docker-compose-file.result == 'success'
        ) &&
        (
          !inputs.run_start_compose_containers ||
          needs.start-compose-containers.result == 'success'
        )
      }}
    needs:
      - check-authorization
      - deploy-changes
      - stop-compose-containers
      - build-docker-compose-file
      - start-compose-containers
    runs-on: ubuntu-latest
    steps:
      - name: Restart non-compose containers
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.NAS_HOST }}
          port: ${{ secrets.NAS_SSH_PORT }}
          username: ${{ secrets.NAS_USER }}
          password: ${{ secrets.NAS_SSH_KEY }}
          script: |
            cd "${{ inputs.repo_path }}"
            echo "${{ secrets.NAS_SSH_KEY }}" | sudo -S /usr/local/bin/docker restart ${{ inputs.container_names }}

  verify-deployment-health:
    if: >-
      ${{
        inputs.health_check != '' &&
        always() &&
        needs.check-authorization.result == 'success' &&
        (
          !inputs.run_pull_changes ||
          needs.deploy-changes.result == 'success'
        ) &&
        (
          !inputs.run_stop_compose_containers ||
          needs.stop-compose-containers.result == 'success'
        ) &&
        (
          !inputs.run_build_docker_compose_file ||
          needs.build-docker-compose-file.result == 'success'
        ) &&
        (
          !inputs.run_start_compose_containers ||
          needs.start-compose-containers.result == 'success'
        ) &&
        (
          !inputs.run_restart_non_compose_containers ||
          inputs.container_names == '' ||
          needs.restart-non-compose-containers.result == 'success'
        )
      }}
    needs:
      - check-authorization
      - deploy-changes
      - stop-compose-containers
      - build-docker-compose-file
      - start-compose-containers
      - restart-non-compose-containers
    runs-on: ubuntu-latest
    steps:
      - name: Determine SSH command timeout for the health check
        id: health-check-timeout
        run: |
          HEALTH_CHECK="${{ inputs.health_check }}"

          rest="${HEALTH_CHECK#*|}"
          timeout_seconds="${rest%%|*}"
          timeout_seconds="${timeout_seconds%%.*}"
          case "$timeout_seconds" in
            ''|*[!0-9]*) timeout_seconds=180 ;;
          esac

          echo "command_timeout=$((timeout_seconds + 60))s" >> "$GITHUB_OUTPUT"

      - name: Verify deployment is healthy
        uses: appleboy/ssh-action@v1.2.5
        env:
          HEALTH_CHECK: ${{ inputs.health_check }}
        with:
          host: ${{ secrets.NAS_HOST }}
          port: ${{ secrets.NAS_SSH_PORT }}
          username: ${{ secrets.NAS_USER }}
          password: ${{ secrets.NAS_SSH_KEY }}
          command_timeout: ${{ steps.health-check-timeout.outputs.command_timeout }}
          envs: HEALTH_CHECK
          script: |
            set -u

            if [ -z "${HEALTH_CHECK:-}" ]; then
              echo "HEALTH_CHECK did not reach the remote shell -- check the envs: entry."
              exit 1
            fi

            url="${HEALTH_CHECK%%|*}"
            rest="${HEALTH_CHECK#*|}"
            timeout_seconds="${rest%%|*}"
            expected_status="${rest##*|}"

            timeout_seconds="${timeout_seconds%%.*}"
            expected_status="${expected_status%%.*}"

            case "$timeout_seconds" in
              ''|*[!0-9]*) timeout_seconds=180 ;;
            esac
            case "$expected_status" in
              ''|*[!0-9]*) expected_status=200 ;;
            esac

            if [ -z "$url" ]; then
              echo "No health URL in HEALTH_CHECK."
              exit 1
            fi

            curl_bin=$(command -v curl 2>/dev/null || true)
            if [ -z "$curl_bin" ]; then
              for candidate in /usr/bin/curl /opt/bin/curl /usr/local/bin/curl; do
                if [ -x "$candidate" ]; then
                  curl_bin="$candidate"
                  break
                fi
              done
            fi
            if [ -z "$curl_bin" ]; then
              echo "No curl on the NAS, cannot verify the deployment."
              exit 1
            fi

            sleep 5

            start=$(date +%s)
            deadline=$(( start + timeout_seconds ))
            echo "Waiting up to ${timeout_seconds}s for $url to answer with $expected_status"

            required_successes=2
            consecutive_successes=0

            while :; do
              code=$("$curl_bin" -sk -L -m 5 -o /dev/null -w '%{http_code}' "$url" 2>/dev/null || echo 000)

              if [ "$code" = "$expected_status" ]; then
                consecutive_successes=$((consecutive_successes + 1))
                echo "  $code ($consecutive_successes/$required_successes consecutive)"
                if [ "$consecutive_successes" -ge "$required_successes" ]; then
                  echo "Healthy after $(( $(date +%s) - start ))s"
                  exit 0
                fi
              else
                consecutive_successes=0
                echo "  still $code"
              fi

              if [ "$(date +%s)" -ge "$deadline" ]; then
                echo "Deployment did not become healthy within ${timeout_seconds}s, last status $code"
                exit 1
              fi

              sleep 5
            done